# 德州扑克前端页面设计

## 项目结构

```
c:\Users\Administrator\Documents\DZ\DeZhou\
├── pages/
│   ├── index/              # 首页 - 房间列表
│   ├── create-room/        # 创建房间
│   ├── room-detail/        # 游戏大厅
│   ├── game-table/         # 游戏进行中
│   └── game-result/        # 游戏结果
├── components/
│   ├── Card.vue            # 扑克牌组件
│   ├── Chip.vue            # 筹码组件
│   ├── PlayerInfo.vue      # 玩家信息组件
│   ├── ActionPanel.vue     # 行动面板组件
│   └── RoomCard.vue        # 房间卡片组件
├── static/
│   └── cards/              # 扑克牌图片
├── utils/
│   ├── game-logic.js       # 游戏逻辑
│   ├── hand-evaluator.js   # 牌型评估
│   └── poker.js           # 扑克牌工具
└── uniCloud-aliyun/
    └── cloudfunctions/     # 云函数
```

## 页面设计

### 1. 首页（房间列表） - pages/index/index.vue

**功能**:
- 显示所有可加入的房间
- 显示房间信息（房间名、玩家数、盲注、状态）
- 创建房间按钮
- 刷新房间列表
- 自动刷新（定时器）

**界面布局**:
```
┌─────────────────────────────────┐
│  德州扑克                        │ ← 导航栏
├─────────────────────────────────┤
│  🔍 搜索房间     [+]创建房间     │ ← 搜索框 + 创建按钮
├─────────────────────────────────┤
│  房间: 红桃A                      │ ← 房间卡片1
│  2/6人  小盲:10  大盲:20         │
│  [加入]                          │
├─────────────────────────────────┤
│  房间: 黑桃K                      │ ← 房间卡片2
│  4/8人  小盲:50  大盲:100        │
│  [加入]                          │
├─────────────────────────────────┤
│  暂无房间                        │ ← 空状态
└─────────────────────────────────┘
```

**数据结构**:
```javascript
data() {
  return {
    rooms: [],        // 房间列表
    loading: false,   // 加载状态
    searchKeyword: '' // 搜索关键词
  }
}
```

**云函数调用**:
- 查询房间列表（数据库查询）

---

### 2. 创建房间页面 - pages/create-room/create-room.vue

**功能**:
- 输入房间名称
- 选择最大玩家数（2-10）
- 设置小盲注和大盲注
- 创建房间
- 返回首页

**界面布局**:
```
┌─────────────────────────────────┐
│  创建房间                        │ ← 导航栏
├─────────────────────────────────┤
│                                  │
│  房间名称:                       │
│  [____________]                   │
│                                  │
│  最大玩家数:                     │
│  [2] [3] [4] [5] [6] [7] [8] [9] [10] │
│                                  │
│  小盲注: [____]                  │
│  大盲注: [____]                  │
│                                  │
│         [创建房间]               │ ← 主按钮
│                                  │
│         [返回]                   │ ← 次要按钮
│                                  │
└─────────────────────────────────┘
```

**数据结构**:
```javascript
data() {
  return {
    roomName: '',
    maxPlayers: 6,
    smallBlind: 10,
    bigBlind: 20
  }
}
```

**云函数调用**:
- `create-room` - 创建房间

---

### 3. 游戏大厅页面 - pages/room-detail/room-detail.vue

**功能**:
- 显示房间信息
- 显示所有玩家列表
- 准备/取消准备按钮
- 等待其他玩家准备
- 房主可以开始游戏
- 离开房间

**界面布局**:
```
┌─────────────────────────────────┐
│  房间: 红桃A     [←]            │ ← 导航栏 + 返回
├─────────────────────────────────┤
│  盲注: 小10/大20  状态: 等待    │
├─────────────────────────────────┤
│                                  │
│  玩家列表:                       │
│  ┌───────────────────────────┐  │
│  │ 🟢 玩家1  [准备] 金币:1000 │  │
│  │ 🔴 玩家2  [准备] 金币:1500 │  │
│  │ 🔴 玩家3  [取消] 金币:800  │  │
│  └───────────────────────────┘  │
│                                  │
│        [开始游戏] [离开房间]    │ ← 房主可见
│        [准备游戏] [离开房间]    │ ← 非房主可见
│                                  │
└─────────────────────────────────┘
```

**状态标识**:
- 🟢 绿色: 已准备
- 🔴 红色: 未准备
- 🟡 黄色: 游戏中（离开后返回）

**数据结构**:
```javascript
data() {
  return {
    room: null,           // 房间信息
    currentUserId: '',    // 当前用户ID
    isOwner: false,       // 是否为房主
    isReady: false,       // 当前用户是否准备
    allReady: false,      // 所有玩家是否准备
    timer: null           // 定时器
  }
}
```

**云函数调用**:
- `ready-game` - 准备/取消准备
- `start-game` - 房主开始游戏
- `leave-room` - 离开房间

---

### 4. 游戏进行中页面 - pages/game-table/game-table.vue

**功能**:
- 显示公共牌
- 显示自己的手牌
- 显示其他玩家状态（头像、金币、下注额）
- 显示当前行动玩家
- 显示底池金额
- 显示当前轮次（翻牌前/翻牌/转牌/河牌）
- 行动面板（check/call/raise/fold/allin）
- 聊天功能（可选）
- 离开游戏（弃牌）

**界面布局**:
```
┌─────────────────────────────────┐
│  房间: 红桃A     [📊]           │ ← 导航栏 + 统计
├─────────────────────────────────┤
│                                  │
│        底池: 150                 │
│                                  │
│  ┌────┬────┬────┬────┬────┐      │
│  │    │    │    │    │    │ ← 公共牌
│  └────┴────┴────┴────┴────┘      │
│                                  │
│  ┌───────────────────────────┐  │
│  │ 👤 玩家3  金币:1500        │  │ ← 上方玩家
│  │     [下注: 50]            │  │
│  │     等待行动               │  │
│  └───────────────────────────┘  │
│                                  │
│  [手牌: ♥A ♠K]                 │ ← 自己的手牌
│  [下注: 30] [当前行动]         │
│                                  │
│  ┌───────────────────────────┐  │
│  │ 👤 玩家1  金币:1200        │  │ ← 下方玩家
│  │     [下注: 20] [弃牌]     │  │
│  └───────────────────────────┘  │
│                                  │
│  轮次: 翻牌前                   │
│                                  │
│  ┌───────────────────────────┐  │
│  │ [过牌] [跟注50]           │  │
│  │ [加注] [100] [全下] [弃牌]│  │ ← 行动面板
│  └───────────────────────────┘  │
│                                  │
└─────────────────────────────────┘
```

**扑克牌显示**:
- 使用 Unicode 字符或图片
- ♠ ♥ ♦ ♣ (黑桃、红桃、方块、梅花)
- A K Q J 10 9 8 7 6 5 4 3 2

**数据结构**:
```javascript
data() {
  return {
    room: null,              // 房间完整信息
    currentPlayerIndex: -1,  // 当前行动玩家索引
    myPlayerIndex: -1,       // 自己的玩家索引
    canAct: false,           // 是否可以行动
    currentBet: 0,           // 当前需要跟注的金额
    round: 'preflop',        // 当前轮次
    timer: null,             // 定时器
    chatMessages: []         // 聊天消息（可选）
  }
}
```

**云函数调用**:
- `player-action` - 玩家行动
- `leave-room` - 离开游戏（弃牌）

---

### 5. 游戏结果页面 - pages/game-result/game-result.vue

**功能**:
- 显示获胜者
- 显示所有玩家的手牌和牌型
- 显示底池分配
- 显示自己的输赢
- 返回房间/返回首页

**界面布局**:
```
┌─────────────────────────────────┐
│  游戏结束                        │ ← 导航栏
├─────────────────────────────────┤
│                                  │
│        🏆 获胜者: 玩家3 🏆        │
│                                  │
│  ┌───────────────────────────┐  │
│  │ 👤 玩家3 [获胜]           │  │
│  │ 手牌: ♥A ♠K              │  │
│  │ 牌型: 一对A              │  │
│  │ 赢得: +200               │  │
│  └───────────────────────────┘  │
│                                  │
│  ┌───────────────────────────┐  │
│  │ 👤 玩家1                 │  │
│  │ 手牌: ♦Q ♣J              │  │
│  │ 牌型: 高牌Q              │  │
│  │ 亏损: -50                │  │
│  └───────────────────────────┘  │
│                                  │
│  底池分配:                       │
│  - 主底池: 150 → 玩家3        │
│                                  │
│  [返回房间] [返回首页]          │
│                                  │
└─────────────────────────────────┘
```

**数据结构**:
```javascript
data() {
  return {
    winners: [],              // 获胜者列表
    potDistribution: [],      // 底池分配
    players: [],              // 所有玩家信息
    myPlayer: null,           // 自己的玩家信息
    handResults: []           // 手牌结果详情
  }
}
```

---

## 组件设计

### 1. Card.vue（扑克牌组件）

**功能**:
- 显示单张扑克牌
- 支持不同花色和点数
- 翻转动画（可选）

**Props**:
```javascript
props: {
  suit: String,   // 花色: 'spade', 'heart', 'diamond', 'club'
  rank: String,   // 点数: 'A', 'K', 'Q', 'J', '10', ..., '2'
  faceUp: {       // 正面朝上
    type: Boolean,
    default: true
  }
}
```

---

### 2. Chip.vue（筹码组件）

**功能**:
- 显示筹码数量
- 不同颜色代表不同面额

**Props**:
```javascript
props: {
  amount: Number,   // 筹码数量
  color: String     // 颜色: 'white', 'red', 'blue', 'green', 'black'
}
```

---

### 3. PlayerInfo.vue（玩家信息组件）

**功能**:
- 显示玩家头像、昵称、金币
- 显示玩家状态（准备/游戏中/弃牌/全下）
- 显示当前下注额
- 高亮当前行动玩家

**Props**:
```javascript
props: {
  player: Object,        // 玩家信息
  isCurrentPlayer: {     // 是否为当前行动玩家
    type: Boolean,
    default: false
  },
  position: String       // 位置: 'top', 'bottom', 'left', 'right'
}
```

---

### 4. ActionPanel.vue（行动面板组件）

**功能**:
- 显示可用的行动按钮
- 处理加注金额输入
- 禁用不可用的行动

**Props**:
```javascript
props: {
  currentBet: Number,    // 当前需要跟注的金额
  myBet: Number,         // 自己已下注金额
  coins: Number,         // 剩余金币
  canCheck: Boolean      // 是否可以过牌
}

emits: ['action']
```

---

### 5. RoomCard.vue（房间卡片组件）

**功能**:
- 显示房间信息
- 显示加入按钮

**Props**:
```javascript
props: {
  room: Object           // 房间信息
}

emits: ['join']
```

---

## 路由配置

在 `pages.json` 中添加新页面：

```json
{
  "pages": [
    {
      "path": "pages/index/index",
      "style": {
        "navigationBarTitleText": "德州扑克"
      }
    },
    {
      "path": "pages/create-room/create-room",
      "style": {
        "navigationBarTitleText": "创建房间"
      }
    },
    {
      "path": "pages/room-detail/room-detail",
      "style": {
        "navigationBarTitleText": "游戏大厅"
      }
    },
    {
      "path": "pages/game-table/game-table",
      "style": {
        "navigationBarTitleText": "游戏进行中"
      }
    },
    {
      "path": "pages/game-result/game-result",
      "style": {
        "navigationBarTitleText": "游戏结果"
      }
    }
  ]
}
```

---

## 实现顺序

1. **第1阶段**: 基础页面
   - ✅ 首页（房间列表）- `pages/index/index.vue`
   - ✅ 创建房间页面 - `pages/create-room/create-room.vue`

2. **第2阶段**: 游戏大厅
   - ✅ 游戏大厅页面 - `pages/room-detail/room-detail.vue`

3. **第3阶段**: 核心游戏
   - ✅ 游戏进行中页面 - `pages/game-table/game-table.vue`
   - ✅ 游戏结果页面 - `pages/game-result/game-result.vue`

4. **第4阶段**: 组件优化
   - 卡牌组件
   - 筹码组件
   - 玩家信息组件

5. **第5阶段**: 高级功能
   - 聊天功能
   - 实时通知
   - 统计数据

---

## 技术要点

1. **实时更新**: 使用定时器定期刷新房间状态
2. **状态管理**: 使用页面 data 管理游戏状态
3. **云函数调用**: 使用 `uniCloud.callFunction`
4. **错误处理**: 显示友好的错误提示
5. **加载状态**: 显示 loading 提示
